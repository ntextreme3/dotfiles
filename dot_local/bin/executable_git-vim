#!/usr/bin/env bash
#
# git-vim: pick files via git-fzf, then open in vim
#

# delegate selection to git-fzf
files=$(git fzf "$@") || exit 0

# if nothing selected, do nothing
if [[ -z "$files" ]]; then
  exit 0
fi

# open in vim
vim $files

